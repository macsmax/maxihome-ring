# Ring doorbell CLI must be installed and authenticated on the host.
# Run: ring-doorbell --auth to set up authentication first.

# Camera names as they appear in Ring (comma-separated)
RING_DEVICES=Front Door,Garden Cam

# Data collection interval in minutes
RING_INTERVAL_MINUTES=15

# Path to store CSV data inside the container
RING_DATA_DIR=/app/data
